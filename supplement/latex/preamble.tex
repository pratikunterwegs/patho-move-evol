
% \usepackage{fontspec}
% use nice fonts if available else use boring defaults

\usepackage{lineno}
% \KOMAoption{fontsize}{10pt}

\renewcommand{\figurename}{Figure S}
\makeatletter
\def\fnum@figure{\figurename\thefigure}
\makeatother

% \IfFontExistsTF{Palatino}{\setmainfont[]{Palatino}}{} 
\usepackage{tgtermes}
% \usepackage{helvet}
% \IfFontExistsTF{Arial}{\setsansfont[]{Arial}}{}
% \IfFontExistsTF{Fira Code}{\setmonofont{Fira Code}}
\usepackage[labelfont=bf,format=plain]{caption}
% \usepackage{setspace}

\PassOptionsToPackage{%
  backend=biber,
  hyperref,
  url=false,
  isbn=false,
  doi=true,
  eprint=false,
  style= oxyear,
  dashed=true,
  maxcitenames=2,
  maxbibnames=99,
  date=year,
  uniquename=false,
  uniquelist=false,
  useprefix=false,
  natbib=true % natbib compatibility mode (\autocite and \citet still work)
}{biblatex}
\usepackage{biblatex}

\linenumbers

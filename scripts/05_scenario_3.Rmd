---
editor_options: 
  chunk_output_type: console
---
  
# Scenario 3: Evolution of Movement Types
  
```{r}
library(data.table)
library(ggplot2)
library(patchwork)
```

## Read data

```{r}
# load files
files = list.files(
  "data/output/small_landscape_60_gen_10000", 
  pattern = "spillover", full.names = T
)
data_sim = vector("list", length = length(files))
repl = c()
spread = c()
for (i in seq(length(files))) {
  load(files[i])
  data_sim[[i]] = data[["gen_data"]]
  repl = c(repl, data[["replicate"]])
  spread = c(spread, data[["clusterSpread"]])
}
```

## Generation level data

### Energy

```{r}
# simulation wise data
data_energy = Map(
  data_sim, repl, spread,
  f = function(le, repl, spread) {
    
    le = Map(le[["pop_data"]], le[["gens"]],
        f = function(l, g) {
          l$gen = g
          l$repl = repl
          l$spread = spread
          l
        }
    ) |>
      rbindlist()
    
    le$repl = repl
    le$spread = spread
    # le
    le[, list(
      mean_energy = mean(energy),
      sd_energy = sd(energy)
    ), by = c("gen", "repl", "spread")]
  }
) |>
  rbindlist()
```

```{r}
plot_energy = ggplot(data_energy)+
  geom_line(
    aes(gen, mean_energy),
    col = "seagreen"
  )+
  facet_wrap(~repl)
```

### Intake

```{r}
data_intake = Map(
  data_sim, repl, spread,
  f = function(le, repl, spread) {
    
    le = Map(le[["pop_data"]], le[["gens"]],
        f = function(l, g) {
          l$gen = g
          l$repl = repl
          l$spread = spread
          l
        }
    ) |>
      rbindlist()
    
    le$repl = repl
    le$spread = spread
    
    get_energy_breakup(le, disease_cost = 0.25)
    
    le[, list(
      mean_intake = mean(intake),
      sd_intake = sd(intake)
    ), by = c("gen", "repl", "spread")]
  }
) |>
  rbindlist()
```

```{r}
ggplot(data_intake)+
  geom_line(
    aes(
      gen, mean_intake
    )
  )+
  facet_grid(~repl)
```


### Movement

```{r}
# simulation wise data
data_movement = Map(
  data_sim, repl, spread,
  f = function(le, repl, spread) {
    
    le = Map(le[["pop_data"]], le[["gens"]],
        f = function(l, g) {
          l$gen = g
          l$repl = repl
          l$spread = spread
          l
        }
    ) |>
      rbindlist()
    
    le$repl = repl
    le$spread = spread
    
    le[, list(
      mean_moved = mean(moved),
      sd_moved = sd(moved)
    ), by = c("gen", "repl", "spread")]
  }
) |>
  rbindlist()
```

```{r}
plot_movement = 
  ggplot(data_movement)+
  geom_line(
    aes(gen, mean_moved),
    col = "steelblue"
  )+
  facet_wrap(~repl)
```

### Infections

```{r}
data_n_infected = Map(
  data_sim, repl, spread, 
  f = function(le, repl, spread) {
    df = data.table(
      gen = le[["gens"]],
      n_infected = le[["n_infected"]]
    )
    df$repl = repl
    df$spread = spread
    df
  }
) |>
  rbindlist()
```

```{r}
plot_infected = 
  ggplot(data_n_infected)+
  geom_line(
    aes(gen, n_infected),
    col = "red"
  )+
  facet_grid(
    ~repl
  )
```

### Associations

```{r}
# simulation wise data
data_assoc = Map(
  data_sim, repl, spread,
  f = function(le, repl, spread) {
    
    le = Map(le[["pop_data"]], le[["gens"]],
        f = function(l, g) {
          l$gen = g
          l$repl = repl
          l$spread = spread
          l
        }
    ) |>
      rbindlist()
    
    le$repl = repl
    le$spread = spread
    
    le[, list(
      mean_assoc = mean(assoc),
      sd_assoc = sd(assoc)
    ), by = c("gen", "repl", "spread")]
  }
) |>
  rbindlist()
```

```{r}
plot_assoc = 
  ggplot(data_assoc)+
  geom_line(
    aes(gen, mean_assoc),
    alpha = 0.5
  )+
  geom_vline(
    xintercept = 6670,
    col = "red"
  )+
  coord_cartesian(
    xlim = 6670 + c(-667, 333)
  )+
  facet_wrap(~repl)
```

```{r}
wrap_plots(
  plot_energy, plot_mvoement, plot_assoc, plot_infected,
  ncol = 1
)
```

## Individual level data

```{r}
# simulation wise data
data = lapply(
  data_sim,
  function(le) {
    Map(le[["pop_data"]], le[["gens"]],
        f = function(l, g) {
          l$id = seq(nrow(l))
          l$gen = g
          l
        }
    ) |>
      rbindlist()
  }
)
```

## Evolution of Movement Types

```{r}
# get movement morphs
data_morphs = Map(
  data, repl, spread,
  f = function(d, repl, spread) {
    d = assign_movement_types(d)
    
    d$repl = repl
    d$spread = spread
    d
  }
)

# bind and count morphs
data_morphs = rbindlist(data_morphs)
data_morphs = data_morphs[, .N, by = c("gen", "move_strat", "repl", "spread")]
```

```{r}
# plot_move_types =
  ggplot(data_morphs)+
  geom_area(
    aes(gen, N, fill = move_strat)
  )+
  geom_vline(
    xintercept = 6670,
    col = 2
  )+
  scale_fill_manual(
    values = c(
      "prey tracking" = "seagreen",
      "prey and handler tracking" = "royalblue",
      "non-handler avoiding" = "red",
      "handler avoiding" = "orange",
      "handler tracking" = "lightblue",
      "other" = "black",
      "NA" = "black"
    ),
    breaks = c(
      "prey tracking",
      "handler tracking",
      "prey and handler tracking",
      "non-handler avoiding",
      "handler avoiding",
      "other"
    ),
    na.value = "black"
  )+
  theme_test()+
  theme(
    strip.background = element_blank(),
    strip.text = element_text(
      face = "italic"
    ),
    legend.position = "top",
    legend.key.width = unit(5, units = "mm"),
    legend.key.height = unit(1, "mm")
  )+
  facet_grid(
    repl ~ spread,
    labeller = label_both
  )+
  coord_cartesian(
    expand = F
  )+
  labs(
    x = "Generation",
    y = "# Agents",
    fill = NULL
  )

ggsave(
  plot_move_types,
  filename = "figures/fig_sc3_move_types.png",
  height = 6, width = 6
)
```

## Social information use

```{r}
# get movement morphs
data_si = Map(
  data, repl, spread,
  f = function(d, repl, spread) {
    get_social_strategy(d)
    
    d = d[, .N, by = c("gen", "strat_social")]
    
    d$repl = repl
    d$spread = spread
    d
  }
)

# bind and count morphs
data_si = rbindlist(data_si)
```

```{r}
# library(biscale)
# plot_si =
  ggplot(data_si)+
  geom_area(
    aes(gen, N, fill = strat_social)
  )+
  geom_vline(
    xintercept = 667,
    col = 2
  )+
  scale_fill_manual(
    values = c(
      "agent avoiding" = "black",
      "agent tracking" = "gold",
      "handler tracking" = "royalblue",
      "non-handler tracking" = "indianred"
    )
  )+
  theme_test()+
  theme(
    strip.background = element_blank(),
    strip.text = element_text(
      face = "italic"
    ),
    legend.position = "top",
    legend.key.width = unit(5, units = "mm"),
    legend.key.height = unit(1, "mm")
  )+
  facet_grid(
    repl ~ spread,
    labeller = label_both
  )+
  coord_cartesian(
    expand = F
  )+
  labs(
    x = "Generation",
    y = "# Agents",
    fill = NULL
  )

ggsave(
  plot_si,
  filename = "figures/fig_sc2_si_use.png",
  height = 6, width = 6
)
```

## Social strategy and outcomes

```{r}
# get social strategy outcomes
data_inf = Map(
  data, repl, spread,
  f = function(d, repl, spread) {
    d = get_social_outcomes(d)
    
    d$repl = repl
    d$spread = spread
    d
  }
)

# bind and count morphs
data_inf = rbindlist(data_inf)
```

```{r}
ggplot(data_inf)+
  geom_point(
    aes(gen, p_inf, col = strat_social),
    alpha = 0.8
  )+
  scale_colour_manual(
    values = c(
      "agent avoiding" = "black",
      "agent tracking" = "gold",
      "handler tracking" = "royalblue",
      "non-handler tracking" = "indianred"
    )
  )+
  scale_y_continuous(
    labels = scales::percent
    # trans = ggallin::ssqrt_trans
  )+
  coord_cartesian(
    xlim = c(6600, 8000)
  )+
  facet_wrap(~repl)+
  labs(
    y = "% infected"
  )
```